import { Canvas, Meta, Controls } from '@storybook/blocks';

import * as  useToggle  from './index.stories';


<Meta of={useToggle} />




# useToggle

This hook is used to toggle between two states. It returns an object with the current state and a function to toggle the state.

### Example :

```tsx
	const {toggle, handleToggle } = useToggle()
	return <Button onClick={handleToggle}>{JSON.stringify(toggle)}</Button>
```


<Canvas of={useToggle.Example} />


## **API**

▸ **useToggle**  (): `{toggle: boolean, handleToggle: function}`

This hook is used to toggle between two states. It returns an object with the current state and a function to toggle the state.

**Parameters:**

None



**Returns:**


<table>

<thead>

<tr>
<th> Name </th>
<th> Type </th>
<th> Description </th>
</tr>
</thead>


<tbody>
<tr>

<td> toggle </td>
<td> `boolean` </td>
<td> The current state of the toggle. </td>

</tr>

<tr>

<td> handleToggle </td>
<td> `function` </td>
<td> The function to toggle the state. </td>

</tr>
</tbody>


</table>

<Controls/>

