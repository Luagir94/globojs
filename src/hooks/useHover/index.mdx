import { Canvas, Meta, Controls } from '@storybook/blocks'

import * as useHover from './index.stories'

import { returns, parameters } from './data'

import Table from '@/components/Table'

<Meta of={useHover} />

# useHover

This hook allows you to detect when the mouse is hovering over an element.

### Example :

```tsx
const ref = useRef<HTMLButtonElement>(null)
const isHovered = useHover(ref)
return (
	<Button ref={ref} className="w-40 font-bold">
		{isHovered ? "It's hovered🎈" : 'Hover!'}
	</Button>
)
```

## **API**

▸ **useHover** (`ref`): _boolean_

This hook allows you to detect when the mouse is hovering over an element.

**Parameters:**

<Table headers={parameters.headers} data={parameters.data} />

**Returns:**

<Table headers={returns.headers} data={returns.data} />

<Canvas of={useHover.Example} />

<Controls />
